apiVersion: v1
kind: Service
metadata:
  name: {{ include "shophub.fullname" . }}-backend
  labels:
    {{- include "shophub.labels" . | nindent 4 }}
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backendPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shophub.selectorLabels" . | nindent 4 }}
    component: backend