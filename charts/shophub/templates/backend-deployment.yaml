apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shophub.fullname" . }}-backend
  labels:
    {{- include "shophub.labels" . | nindent 4 }}
    component: backend
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "shophub.selectorLabels" . | nindent 6 }}
      component: backend
  template:
    metadata:
      labels:
        {{- include "shophub.selectorLabels" . | nindent 8 }}
        component: backend
    spec:
      containers:
      - name: backend
        image: "{{ .Values.backendImage }}:{{ .Values.backendTag }}"
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: {{ .Values.backendPort }}
          protocol: TCP
        env:
        - name: NODE_ENV
          value: {{ .Values.env.NODE_ENV }}
        - name: LOG_LEVEL
          value: {{ .Values.env.LOG_LEVEL }}
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ .Values.databaseSecretName }}
              key: {{ .Values.databaseSecretKey }}
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "shophub.fullname" . }}-secrets
              key: jwt-secret
        - name: SIWE_DOMAIN
          value: {{ .Values.siweConfig.domain }}
        - name: SIWE_ORIGIN
          value: {{ .Values.siweConfig.origin }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
